name: Publish new theme version

on:
  push:
    branches: [ $default-branch ]

jobs:
  publish:
    uses: juve33/.github/.github/workflows/wp-publish-theme-update.yml@main
    permissions:
      contents: write
      pages: write
      id-token: write